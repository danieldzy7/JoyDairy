services:
  - type: web
    name: joy-dairy-fullstack
    env: node
    plan: free
    buildCommand: npm install && npm install --prefix client && npm run build --prefix client
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        fromService:
          type: web
          name: joy-dairy-fullstack
          property: port
      - key: MONGODB_URI
        sync: false # You'll set this manually in Render dashboard
      - key: JWT_SECRET
        generateValue: true # Render will generate a secure secret
    autoDeploy: false
